
export const calculadorExponentes = ({ base, exponente }) => {
    base = parseInt(base)
    exponente = parseInt(exponente)

    const resultado = Math.pow(base, exponente);
    return (resultado)
}



